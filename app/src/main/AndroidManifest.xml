<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shanejansen.mvptest">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".ui.mainactivity.main.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
        android:name=".ui.secondactivity.SecondActivity"
        android:parentActivityName=".ui.mainactivity.main.MainActivity" />

    <activity
        android:name=".ui.multifragmentactivity.MultiFragmentActivity"
        android:parentActivityName=".ui.mainactivity.main.MainActivity" />

    <activity
        android:name=".ui.mainactivity.rxjava.RxJavaFragment"
        android:parentActivityName=".ui.mainactivity.main.MainActivity" />
  </application>

</manifest>
